{
  "name": "static-site",
  "version": "1.0.0",
  "description": "A simple static website with Markdown support",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch"
  },
  "dependencies": {
    "marked": "^12.0.0",
    "fs-extra": "^11.2.0",
    "chokidar": "^3.6.0"
  }
} 